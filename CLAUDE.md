@AGENTS.md

## Claude Code
- Mode plan obligatoire avant de toucher `/games/*/kernel_*.asm` — chemin critique du projet, aucune modification à l'aveugle
- Ne jamais cocher une porte dans `backlog.md` sans confirmation explicite de l'utilisateur — ce sont des décisions de possession de projet, pas des faits vérifiables par le code (voir backlog.md § Protocole IA)
- En cas de session `/compact` : les fichiers d'instructions sont rechargés automatiquement, mais relire `backlog.md` en entier avant de reprendre une tâche en cours — le résumé de conversation ne contient pas l'état des portes
